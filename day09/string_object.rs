fn main() {
	// Defining a empty string object
	let empty_string = String::new();
	println!("The string is : {}", empty_string);

	// Defining a string object with some value
	let content_string = String::from("Might be an object too.");
	println!("The string is: {}", content_string);
}